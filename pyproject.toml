[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "master-thesis"
version = "0.1.0"
description = "Span classification for decoder-only transformer models"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
